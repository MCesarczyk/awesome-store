query ProductsGetList($first: Int, $skip: Int) {
  products(first: $first, skip: $skip) {
    id
    name
    slug
    categories {
      id
      name
    }
    description
    images {
      id
      url
      alt
    }
    price
  }
}
