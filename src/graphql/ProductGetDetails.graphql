query ProductGetDetails($id: ID!) {
  product(id: $id) {
    id
    name
    slug
    categories {
      id
      name
      slug
      description
    }
    collections {
      id
      name
      slug
      description
    }
    description
    images {
      id
      url
      alt
    }
    price
  }
}
