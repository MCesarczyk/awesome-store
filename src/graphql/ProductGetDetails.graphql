query ProductGetDetails($id: ID!) {
  product(id: $id) {
    id
    name
    slug
    categories {
      id
      name
    }
    collections {
      id
      name
    }
    description
    images {
      id
      url
      alt
    }
    price
  }
}
